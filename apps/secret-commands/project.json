{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/secret-commands/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/node:webpack",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/secret-commands",
        "main": "apps/secret-commands/src/main.ts",
        "tsConfig": "apps/secret-commands/tsconfig.app.json",
        "assets": ["apps/secret-commands/src/assets"]
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false,
          "fileReplacements": [
            {
              "replace": "apps/secret-commands/src/environments/environment.ts",
              "with": "apps/secret-commands/src/environments/environment.prod.ts"
            }
          ]
        }
      }
    },
    "serve": {
      "executor": "@nrwl/node:node",
      "options": {
        "buildTarget": "secret-commands:build"
      },
      "configurations": {
        "production": {
          "buildTarget": "secret-commands:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/secret-commands/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/secret-commands"],
      "options": {
        "jestConfig": "apps/secret-commands/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
