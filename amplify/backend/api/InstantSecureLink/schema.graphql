type LinkModel @model @auth(rules: [{ allow: public }]) {
    id: ID! @primaryKey
    secretId: String
    secretKey: String
    creatorUserID: String
    recipients: [String]
    views: Int
    burnt: Boolean
    ttl: Int
    passphrase: String
    internal: Boolean
}
